# CLAUDE.md

Read `AGENTS.md`. The workflow contract is in `zeroroot-ai/.github`. The
writing rules for every page in this repo are in `AGENTS.md` under
"Writing rules". `pnpm build` runs `check:docs`, which enforces the rules a
machine can check.
